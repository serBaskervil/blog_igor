<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :Імя %><br>
		<%= f.text_field :body %>
	</p>

	<p>
		<%= f.label :Коментар %><br>
		<%= f.text_area :body %>
	</p>
	<br>
	<p>
		<%= f.submit %>
	</p>
	<%  end %>